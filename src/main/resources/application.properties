# Quiz Application Configuration
app.name=Quiz App for Desktop
app.version=1.0.0

# Database Configuration (override via environment variables)
# QUIZ_DB_URL, QUIZ_DB_USER, QUIZ_DB_PASSWORD
db.url=jdbc:mysql://localhost:3306/nlogin
db.user=root
db.password=
